apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: research-workspace
spec:
  rules:
    - host: research.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: research-api
                port:
                  number: 8000

